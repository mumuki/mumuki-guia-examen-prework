describe("", () => {
  it("alturaArbolUtopico(0)", () => {
    assert.equal(alturaArbolUtopico(0), 1);
  });
  it("alturaArbolUtopico(1)", () => {
    assert.equal(alturaArbolUtopico(1), 2;
  });
  it("alturaArbolUtopico(2)", () => {
    assert.equal(alturaArbolUtopico(2), 3);
  });
  it("alturaArbolUtopico(3)", () => {
    assert.equal(alturaArbolUtopico(3), 6);
  });
  it("alturaArbolUtopico(4)", () => {
    assert.equal(alturaArbolUtopico(4), 7);
  });
  it("alturaArbolUtopico(5)", () => {
    assert.equal(alturaArbolUtopico(5), 14);
  });
  it("alturaArbolUtopico(6)", () => {
    assert.equal(alturaArbolUtopico(6), 15);
  });
})